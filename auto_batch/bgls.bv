N := 200
l := 5

BEGIN :: types
pk := G2
g2 := G2
END :: types

BEGIN :: signature
sig := { prod{i:=1, l} on sig_i }
END :: signature

verify := e(sig, g2) == (prod{i := 1, l} on e(H(M_i, G1), pk_i))
