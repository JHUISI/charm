N := 200
l := 5

BEGIN :: types
sig := G1
M := str
pk := G2
g2 := G2
END :: types

BEGIN :: signature
sig
END :: signature

BEGIN :: constant
g2, pk
END :: constant

# dot product of BLS signatures [i=1, L]
BEGIN :: precompute
sig := { prod{i:=1, l} on bsig_i }
abc := b-a
END :: precompute

BEGIN :: transform
2, 3, 4
END :: transform

verify := { e(sig, g2) == (prod{i := 1, l} on e(H(M_i, G1), pk_i)) }
