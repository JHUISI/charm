
N := 200

BEGIN :: types
s := ZR
h := G1
S1 := GT
S2 := G1
pk := G1
a := ZR
END :: types

#S1 := e(h, g2)^s
#S2 := (sk^a) * (h^s)

BEGIN :: precompute
a := H(M_i | S1_i, ZR)
END :: precompute

BEGIN :: constant
P, g2
END :: constant

verify := { e(S2, g2) == ( e(pk, P)^a ) * S1 }
